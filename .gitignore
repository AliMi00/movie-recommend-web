# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Environment & Secrets — never commit real values, only .env.example
.env
.env.*
!.env.example
*.env
docker-compose.override.yml

# Any accidental cert/key material
*.pem
*.key
*.secret
*.p12
*.jks
*.keystore

# Android signing & Firebase config — these must never reach a public repo.
# android/.gitignore covers most of it; these are belt-and-braces at the root
# so a file created outside that directory is still caught.
key.properties
google-services.json
android/local.properties

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
# Saved plans embed the variable values, secrets included.
*.tfplan
!*.tfvars.example
crash.log
crash.*.log
.terraform.lock.hcl

# Local development
.vscode/
.idea/
*.user

# Personal writing drafts — never publish these with the repo
drafts/
